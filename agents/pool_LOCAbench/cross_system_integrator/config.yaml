name: cross_system_integrator
role: integrator
description: "Designs entity mappings, dependency order, and consistency constraints when a task spans two or more systems; does not execute writes."
model: claude-sonnet-4.6
temperature: 0.2
max_tokens: 24576
max_steps: 50
tools:
  - read_file
  - bash
  - loca_mcp
