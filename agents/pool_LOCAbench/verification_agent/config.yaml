name: verification_agent
role: verifier
description: "Independently checks completeness, artifact correctness, external postconditions, and side effects without repairing the result itself."
model: claude-sonnet-4.6
temperature: 0.2
max_tokens: 24576
max_steps: 50
tools:
  - read_file
  - bash
  - loca_mcp
