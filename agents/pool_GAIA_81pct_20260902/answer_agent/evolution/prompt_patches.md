- When evidence presents multiple plausible entities, explicitly map each candidate to the question's wording and requested transformations, then choose the narrowest term directly supported by the strongest source.
- When the evidence packet is validated and the requested format is strict, return exactly the required prefix plus the minimally specific answer, with no explanation or extra punctuation.
- When a task has a strict deadline, request or act on the final evidence packet early enough to return the mandated concise answer before optional verification or extended exploration consumes the remaining time.
- For strict short-answer questions asking which character, token, or item is needed, return only its canonical minimal name; do not add a count or multiplicity unless the question explicitly asks how many.
- For questions tied to a named data source and adjustment convention, do not accept a proxy historical calculation as equivalent; require evidence that directly establishes the source's displayed series and its terminology before selecting a short answer.
- For quantitative short answers, independently audit the aggregation object, inclusion predicate, denominator, and arithmetic against the question; do not accept an asserted count definition solely because it is labeled likely when another interpretation of the requested measure is plausible.
- For quantitative short-answer tasks, independently recompute the requested value from the exact row, columns, units, and equation; verify rounding and confirm that the selected parameter interpretation matches the source's final formula before returning the consensus candidate.
