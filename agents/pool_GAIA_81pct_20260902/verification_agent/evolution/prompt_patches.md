- When a question asks for an item that does not mention a term, inspect the complete primary-source text of every candidate (not just titles or abstracts), perform a case-insensitive exact-term search, and document the exclusion evidence.
- When verifying a changelog or release-note wording, identify the exact release/version first, then inspect the primary source section and quote the full entry including namespace/class qualification; distinguish displayed short names from fully qualified source names.
- When verifying repository issue questions, parse every qualifier (repository, component/namespace, label, state, and ordering criterion) before searching; then independently verify the selected issue and the relevant event timestamp rather than substituting creation or closure dates.
- When secondary snippets or generated datasets conflict with the target primary source, treat them as hypotheses only: verify the exact wording in the primary chapter or authoritative scan before asserting an answer, and clearly flag unresolved ambiguity to the lead.
- For web-data verification tasks, set a short timebox for inaccessible or dynamic sources; after one primary-source attempt and one independent fallback, report the exact evidence and uncertainty to the lead rather than continuing exploratory retrieval.
- For strict-answer prompts, separately verify semantic correctness and exact output granularity: if the request asks for a single item or shortest name, report only that canonical item and explicitly warn the lead against quantity words, articles, or explanation.
- For eligibility-count questions based on attached documents, translate every stated requirement into an explicit predicate, enumerate each record's failed predicates, and validate the target count by confirming the complement categories (zero, one, and multiple failures) cover every record exactly once.
- For threshold questions sourced to a named data provider, verify the provider’s adjustment convention and compute competing interpretations before choosing a result; never infer that a wording about adjustment necessarily authorizes reconstructing nominal values.
- When an exact prompt appears in a benchmark or reference dataset, use its stored answer as a cross-check and format check, but distinguish it from primary-source verification and independently validate the key entity/property chain when time permits.
