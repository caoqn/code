- When a question hinges on a source not containing a term, verify absence against the complete machine-readable/full-text representation rather than relying on snippets or visual page search; document the exact endpoint and search term.
- For dated release-note or changelog questions, verify the relevant version/date in an authoritative rendered or raw source, locate the exact section and entry, and report the command or identifier exactly as written with a direct source URL.
- When researching historical repository labels, first enumerate candidates via the authoritative search/API, then inspect each candidate's event timeline; account for renamed labels by matching historical label names to current labels and report the exact event timestamp.
- For fact-finding tasks with a fixed time budget, time-box source discovery: pursue the most direct authoritative dataset or page first, send exact intermediate values and calculation method as soon as found, and after two unproductive retrieval paths report the blockage rather than continuing broad searches.
- For threshold questions tied to a named financial site, establish whether the requested series is source-displayed or a reconstructed nominal series before calculating; never let an ambiguous parenthetical override the named source without direct evidence of that source's adjustment convention.
- When a data source groups records for display, verify the exact public-page counting unit, pagination, and date-selection behavior before aggregating; if an independently reported result conflicts, investigate scope and representation rather than treating documentation alone as conclusive.
