name: repo_analyst
description: "Repository analysis specialist — maps call paths, module boundaries, and likely root-cause locations before code changes."
model: claude-sonnet-4.6
temperature: 0.2
max_tokens: 32768
max_steps: 70
tools:
  - docker_bash
  - read_file
