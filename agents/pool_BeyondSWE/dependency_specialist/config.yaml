name: dependency_specialist
description: "Dependency migration specialist — traces changed APIs and updates compatible call sites without downgrading dependencies."
model: claude-sonnet-4.6
temperature: 0.2
max_tokens: 32768
max_steps: 90
tools:
  - docker_bash
  - docker_str_replace_editor
  - read_file
