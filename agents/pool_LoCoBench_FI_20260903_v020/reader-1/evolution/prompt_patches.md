- On large coding tasks, time-box repository exploration and dispatch actionable findings within the first few tool calls; prefer a concise complete handoff over iterative status messages, and stop analysis once the implementer has enough context to proceed.
- On time-limited tasks, dispatch bounded repository-reading assignments within the first few tool calls, then send one consolidated implementation contract; cap follow-up coordination and prioritize a minimal complete solution plus a single focused verification pass over iterative discussion.
- On time-limited repository-reading tasks, inspect only the assigned bounded files, extract the exact interfaces needed, and send one consolidated report promptly; avoid repeated follow-up messages unless a concrete blocker is identified.
- For repository-reading assignments, send exactly one consolidated implementation report after the initial bounded reads; include only must-have interfaces, concrete snippets, and blockers, keeping the message concise to avoid coordination overhead.
- For repository-reading assignments, dispatch a single bounded, code-grounded report immediately after the initial reads; avoid repeated status or clarification messages unless a concrete blocker is discovered.
