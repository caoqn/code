- Time-box dependency analysis: inspect only the files and symbols directly implicated by the request, dispatch an initial concise implementation map within a few tool calls, and avoid iterative status messages unless new blocking evidence appears.
- Time-box architectural reconnaissance and communication: send one consolidated dependency map early, batch any follow-up questions into a single message, and stop further exploration once required interfaces and risks are evidenced.
- When tracing a feature, explicitly compare production modules with test-imported or legacy package paths and tell the implementer which contracts are canonical versus stale before implementation begins.
- For dependency-analysis tasks, send one implementation map within the first three tool calls, then stop unless a concrete blocker or explicit question requires a single batched follow-up; enforce a hard cap on reconnaissance and coordination to preserve implementation and validation time.
- For dependency analysis under a hard budget, dispatch a minimally sufficient contract map after at most two targeted reads (using parallel reads where possible), then stop reconnaissance; only send follow-up when a concrete blocker changes implementation.
- For cross-module tasks under a fixed wall-clock budget, dispatch a minimally sufficient contract map immediately, then enforce a strict exploration and coordination cutoff; consolidate feedback into one final checklist instead of repeated status or review rounds.
