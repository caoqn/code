- Dispatch bounded reconnaissance to non-overlapping agents within the first three tool calls, including explicit questions and a deadline; do not wait for exhaustive exploration.
- Set a hard reconnaissance/coordination cutoff. Once every requirement has concrete repository evidence, stop reading and send one prioritized implementation contract rather than iterative updates.
- Identify the canonical runtime entrypoint and trace requested behavior end-to-end across models, services, persistence, transports, factories, and tests; label duplicate or legacy paths and compatibility decisions explicitly.
- Use a single final verification checklist covering exact contracts, route registration, optional-import fallbacks, edge-case/error semantics, tests or static checks, and source/artifact cleanliness.
- When task wording is incomplete or inferred from context, explicitly separate confirmed requirements from hypotheses, inspect all relevant tests and serializers, and deliver a single evidence-backed contract that includes malformed-input, compatibility, and duplicate-entrypoint behavior.
- After a feature is implemented, independently verify that every required public symbol is exported and importable, including symbols defined near or after an existing __all__; inspect optional-import fallbacks for undefined exception names and confirm GET/POST error semantics separately.
- When repository evidence reveals tests or generated artifacts with exact constructor/import/return contracts, treat those literals as authoritative and immediately reconcile them against the initially identified canonical path before advising implementation.
- When requirements introduce a new cross-cutting symbol but exact hidden-test names are uncertain, explicitly separate confirmed contracts from hypotheses and recommend a minimal compatibility alias strategy rather than repeatedly speculating.
