- Time-box repository verification: inspect required output files and key contracts early, then issue a concise pass/fail checklist; avoid repeated broad reads or iterative messaging when the submission is already substantially complete.
- Initiate delegated work within the first few actions using a minimal requirement summary; do not delay dispatch while seeking exhaustive repository understanding.
- Consolidate verification findings into one prioritized message per recipient, and stop after confirming blocking contracts; avoid repeated status polling or incremental rechecks unless new evidence appears.
- Before issuing a verification pass, extract every exact contract element (path, method, status, payload fields/values, auth behavior) from the task and compare each explicitly against both implementation and documentation; do not rely on inferred defaults.
- During verification of cross-cutting features, inspect every application entrypoint and integration path (not just the newly added module), then check edge cases in parsing, configuration precedence, and failure responses against explicit contracts.
- When verifying cross-service integrations, inspect the relevant existing tests, entrypoints, schemas, and configuration to derive canonical endpoint paths and payload contracts; do not approve based only on the new files.
- During final verification, treat explicitly requested artifact names and paths as hard contracts: enumerate outputs, flag substitutions or renamed tests, and recheck cleanliness after every static command that may generate artifacts.
- Set an explicit implementation and review cutoff at the start of each task: dispatch immediately, gather one consolidated requirement checklist, and stop exploratory reads or message loops once blocking contracts are verified.
- At task start, set a hard time/message budget and a single implementation cutoff; dispatch immediately, then limit coordination to one consolidated requirements message and one final verification round, avoiding repeated status polls or incremental feedback unless a new blocking defect appears.
- Before issuing a final verification report, re-read the latest implementation and documentation sections after any teammate update, and quote exact status codes and payload strings from current files to avoid stale-contract reports.
- During forced or time-boxed reviews, perform exactly one consolidated inspection of required artifacts and contracts, then send a single prioritized report per recipient; do not issue incremental status messages or repeat checks unless a new blocking defect is evidenced.
- Set a strict message budget for verification (initial contract checklist plus one final report); defer non-blocking observations and avoid forwarding the same issue to multiple agents more than once.
- When verifying a new API or plugin, inspect at least one invalid-input and unknown-registry-key path in addition to the happy-path route, and report robustness gaps separately from contract blockers.
- In forced or time-boxed tasks, perform a single bounded review cycle: dispatch immediately, inspect only canonical artifacts, and stop after one consolidated findings report; do not repeat status polling or exploratory reads once core contracts are checked.
- For time-boxed multi-agent tasks, cap coordination to an initial requirements/dispatch message and one final consolidated report; perform bounded parallel artifact inspection, avoid status polling and follow-up loops, and stop once blocking contracts are checked even if optional validation remains.
- During final verification, explicitly assess robustness and maintainability beyond happy-path contracts: inspect malformed inputs, dependency-unavailable behavior, shutdown idempotence, and type/serialization edge cases, then report these separately from blockers.
- When verifying generated APIs, explicitly inspect malformed/empty inputs, missing persisted artifacts, serialization round-trips, and dependency/import fallbacks; distinguish contract blockers from robustness gaps and report each with concrete evidence.
