- Dispatch agents within the first few coordination calls using a concise task brief; avoid extended preliminary exploration and redundant status/message exchanges.
- After the initial requirement scan, dispatch agents within at most three coordination calls and avoid redundant status relays; set a fixed implementation/review cutoff and submit existing artifacts when the cutoff is reached.
- Before finalizing a feature, independently inspect every changed artifact and compare it against each acceptance criterion, especially exact response schemas and documentation examples; do not rely solely on implementation reports.
- Before approving implementation, verify that feature edits preserve the repository's existing public schema, service constructor, and resolver behavior; require additive integration or explicit compatibility adapters rather than replacing scaffolding.
- When a repository contains parallel domain and REST service implementations, explicitly map both interfaces and decide which is canonical from tests/application wiring before editing; implement additive compatibility changes in each required surface and verify the exact externally requested endpoint with an end-to-end test.
- For large feature tasks, dispatch the implementer and evidence agents immediately after a single scope scan, set explicit evidence and implementation cutoffs, and limit coordination to one status checkpoint plus final artifact verification; proceed with core evidence rather than waiting for every optional report.
- Set an explicit implementation and verification cutoff at the start of every task: dispatch agents within three coordination calls, allow one evidence round and one final review round, and submit available artifacts when the cutoff is reached instead of continuing iterative message exchanges.
- Impose a strict coordination budget: dispatch all agents immediately after a single scope scan, send one complete assignment per agent, allow at most one status checkpoint and one final verification round, and avoid relaying duplicate updates or requesting iterative confirmations.
- Before final submission, independently grep all generated artifacts for exact acceptance literals (status payloads, header names, environment variables) and remove build artifacts such as bytecode caches; do not rely on teammate summaries.
- For resource-limited tasks, dispatch all needed agents in one batch after a single scope scan, give each a complete bounded assignment, and prohibit follow-up clarification/status messages until the single implementation cutoff; then perform one artifact verification and finalize immediately.
- Before finalizing feature work, verify every acceptance criterion against the actual externally visible interface (request models, route parameters, response schemas), not merely registries or documentation; if a requirement names a selectable option, ensure the live endpoint accepts and uses that option.
- For time-bounded tasks, perform one minimal scope scan, dispatch all required agents immediately with complete acceptance criteria and a hard cutoff, then allow only one implementation status and one verification pass; avoid relaying intermediate findings or requesting repeated confirmations.
- For resource-constrained tasks, dispatch a minimal team in one batch after a single scope scan, enforce a hard message budget (one assignment and one checkpoint per agent), and stop coordination once core evidence and artifacts are available; reserve the final window for independent artifact verification and submission.
- After implementation, independently inspect every changed function and grep exact acceptance literals, including success sentinel values and resource cleanup calls, before requesting finalization.
