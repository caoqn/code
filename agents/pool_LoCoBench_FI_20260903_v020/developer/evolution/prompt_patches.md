- Time-box implementation coordination: dispatch requests within a few tool calls, consolidate feedback into single concise messages, and begin coding as soon as core interfaces are known; reserve at most one verification iteration to avoid exhausting the task budget.
- Time-box collaboration: dispatch targeted requests within the first few calls, begin implementation as soon as core interfaces are known, and consolidate feedback into one review cycle; avoid repeated status pings and incremental follow-up messages unless a blocking defect is identified.
- Before implementing an endpoint, explicitly record the exact path, status code, response keys/values, and dependency requirements from the task and cross-check them against all router mounting surfaces; verify the final artifact preserves existing behavior rather than unnecessarily rewriting integration modules.
- When a repository contains duplicate framework entry points, verify and integrate the feature into the canonical factory as well as any explicitly named endpoint module, while leaving clearly legacy schemas untouched.
- Before writing replacement modules, inspect and preserve existing interfaces, imports, and entrypoints; implement incrementally against the repository's canonical patterns, and verify every changed file's callers and integration surfaces.
- Before finalizing cross-file feature edits, inspect the complete diff and run targeted contract checks; avoid broad text substitutions that can alter neighboring methods, and verify both direct service behavior and the actual HTTP integration path.
- For time-bounded tasks, establish a hard execution budget at the outset: dispatch bounded reads immediately, begin implementation after the first sufficient interface report, and reserve one short verification pass; stop exploratory coordination once core contracts are known.
- When task budgets are tight, cap coordination to one initial dispatch per needed role and one consolidated review; start implementation after the first sufficient interface report, avoid repeated status polling, and reserve time for a single final verification pass.
- After receiving a sufficient architecture report, immediately produce the minimal canonical implementation and run one static contract check; defer optional duplicate compatibility artifacts and documentation until core endpoint reachability is verified.
- Before finalizing cross-cutting changes, preserve full existing artifacts and verify runtime contracts against every canonical and alternate integration path; isolate optional import failures and run a focused static cleanliness check.
- Set an explicit wall-clock and message budget at task start; dispatch targeted requests immediately, begin implementation after the first adequate interface report, and allow at most one consolidated review pass before finalizing.
- When copying context artifacts into the solution, inspect and normalize generated content immediately (remove formatting fences, preserve exact paths), then run a focused syntax check and artifact scan before reporting completion.
- For feature tasks under a fixed wall-clock budget, impose a strict two-phase plan: dispatch bounded reconnaissance immediately, then implement after the first sufficient report; allow only one consolidated review/verification round and stop all further coordination once acceptance-critical artifacts are produced.
- Before pruning apparently legacy or duplicate artifacts, confirm the task's expected output manifest and preserve any explicitly requested entrypoints; treat hidden artifact-contract requirements as first-class alongside runtime correctness.
- For medium feature tasks, enforce a hard two-pass workflow: dispatch targeted reconnaissance immediately, implement after the first sufficient interface report, and allow only one consolidated review; cap total coordination messages and stop status polling once acceptance-critical artifacts are written.
- For fixed-budget tasks, enforce a strict coordination cap: dispatch each role once, begin implementation after the first sufficient interface report, send at most one consolidated review request, and avoid follow-up/status messages unless a blocking defect is identified.
- When modifying context-derived modules, first copy the complete source unchanged, then apply narrowly scoped additive edits; run a diff-oriented check to ensure unrelated entrypoints, optional dependencies, and public methods remain intact.
- When implementing tuple-based validation APIs, extract and preserve exact success/failure sentinel values (especially None versus empty strings) and update every validator/helper consistently; verify annotations and all success paths with targeted grep before finalizing.
- Before finalizing a cross-layer feature, grep for duplicate implementations and stale documentation schemas, then consolidate to one canonical implementation and compatibility re-exports.
- Before responding to defect reports, inspect the current artifact directly and reconcile stale reports; when modifying an existing module, preserve all unrelated public classes and helper APIs, applying narrowly scoped edits rather than replacing the file.
- For cross-cutting features, treat focused tests as acceptance artifacts: inspect existing test conventions early, add coverage for each new public contract, and run those tests rather than relying solely on syntax compilation.
