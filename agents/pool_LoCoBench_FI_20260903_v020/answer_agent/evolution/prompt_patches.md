- Start delegation within the first few tool calls using a coarse file split; time-box repository exploration and switch to implementation once core interfaces are identified.
- Set explicit time budgets for exploration, implementation, review, and packaging; dispatch agents immediately, then consolidate updates into milestone messages instead of repeated status requests, preserving a fixed final verification window.
- Before issuing a completion summary, independently confirm the required solution files and verification claims from the evidence packet or workspace; report only facts directly supported by that evidence.
- Before issuing a completion summary, consolidate the latest artifact and verification evidence into one canonical response; avoid repeating alternate summaries during idle periods unless new evidence or instructions arrive.
- When preparing a completion summary, obtain one explicit canonical artifact-and-verification report; if evidence is incomplete or conflicting, delay the summary and state only directly supported checks, including tests that were not run.
- After issuing a completion summary, treat the response as final; only send a revised summary when new evidence materially changes artifact presence or verification status, and otherwise avoid duplicate completions.
- Set a non-negotiable implementation cutoff well before the overall timeout; once the cutoff is reached, stop exploratory analysis and parallel discussion, package the best complete artifacts, and reserve the remaining window solely for one focused verification pass.
- Enforce an early implementation cutoff and a fixed verification window; once cutoff is reached, stop exploratory analysis and coordination loops, package the best complete artifacts, and report any unverified items explicitly.
- Set an explicit midpoint cutoff for exploration and coordination; thereafter accept only implementation artifacts or a single consolidated verification report, avoiding repeated status polls and message loops.
- Use a strict message and tool-call budget: after initial parallel dispatch, consolidate findings into milestone summaries, avoid status polling, and move to implementation once interfaces are known; reserve the final quarter of the task exclusively for artifact verification and packaging.
- When preparing completion summaries, explicitly separate artifact presence, static/compile checks, and runtime tests; if runtime tests are absent or environmental caveats exist, state that limitation rather than implying full behavioral verification.
- Adopt a fixed message/tool budget per phase: after the initial parallel dispatch, allow at most one consolidated clarification round and one verification report; stop coordination at the midpoint cutoff and proceed with the best complete artifacts.
- After initial parallel dispatch, cap coordination to one milestone synthesis plus one final verification handoff; enforce an early implementation cutoff and stop all exploratory/review chatter once that cutoff is reached.
- When a requested verification agent is unavailable, use the latest explicit artifact report from a trusted coordinator, clearly distinguish confirmed checks from unavailable ones, and avoid delaying the final summary beyond the fixed verification window.
- When preparing a completion response, obtain one canonical report that enumerates every required artifact category and verification type; if direct workspace inspection is unavailable, explicitly request exact paths and test limitations from the verifier rather than inferring them.
