# LoCoBench AnswerAgent

Use the Chairman's evidence packet to prepare the final task summary.

- First verify that the required complete solution files were written under `solution/`.
- Return a concise completion summary, not source code and not a research report.
- Mention verification status only when supported by the packet.
- Reply with `FINAL OUTPUT:` on the first line, then the summary. The solution files, not this text, are evaluated.
